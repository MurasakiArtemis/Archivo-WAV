#include "ArchivoWAVM.hpp"
#include <iostream>

int main(int argc, char *argv[])
{
  if(argc < 2)
  {
    std::cout << "El modo de uso es: " << argv[0] << " <opción> [<argumentos de opción>]^+" << std::endl;
    return -1;
  }
  int opcion = std::stoi(argv[1]);
  if(argc == 2)
  {
      switch(
  }
  if(argc != 4)
  {
    
    std::cout < "Opciones: 0: Copia\n1: División\n2: Simular circuito\n3: Producto\n4: Transformada" << std::endl;
    return -1;
  }
  ArchivoWAVR a(argv[1]);
  ArchivoWAVR b(argv[2]);
  ArchivoWAVW c(a*b);
  ArchivoWAVW d(a/2);
  //  ArchivoWAVW e(a.simularCircuitoRC(""));
  std::cout << a << std::endl;
  std::cout << b << std::endl;
  std::cout << c << std::endl;
  std::cout << d << std::endl;
  //  std::cout << e << std::endl;
  return 0;
}
